// Settings/Tasks defined in localBuild.sbt take precedence over those defined in build.sbt for the current sub-project.

// provides re-start and re-stop sbt commands to start and stop the application in background.
// Revolver.settings
